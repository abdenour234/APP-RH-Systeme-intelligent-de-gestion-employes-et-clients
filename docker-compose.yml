version: '3.8'
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://aws-0-eu-west-3.pooler.supabase.com:6543/postgres?ssl=true&sslmode=require
      - SPRING_DATASOURCE_USERNAME=postgres.gcftmbhqlkqcamiwjoky
      - SPRING_DATASOURCE_PASSWORD=Oujda@oujda
    
    volumes:
      - ./backend:/app/backend
    networks:
      - app-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "80:80"
    volumes:
      - ./frontend:/app/frontend
    depends_on:
      - backend
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
